server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://${PGHOST:ep-orange-wind-884154.ap-southeast-1.aws.neon.tech}:${PGPORT:5432}/${PGDATABASE:postgres}
    username: ${PGUSER:fl0user}
    password: ${PGPASSWORD:S6WndIcNJi7B}
  jpa:
    hibernate:
      ddl-auto: update

short-url:
  base-url: ${BASE_URL:http://localhost:8080}
  allowed-characters: ${ALLOWED_CHARS:abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789}
  key-length: ${KEY_LENGTH:6}

chrome-extension-id: ${CHROME_EXTENSION_ID:<your_chrome_extension_id>}